# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# PostgreSQL connection string (required for all db commands)
DATABASE_URL=postgresql://user:password@localhost:5432/chronopay

# HTTP server port (optional, defaults to 3001)
PORT=3001
